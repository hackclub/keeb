---
import { getCollection } from 'astro:content';

const docs = await getCollection('docs');
const first = docs.sort((a, b) => a.data.order - b.data.order)[0];

return Astro.redirect(`/docs/${first.id}`);
---
